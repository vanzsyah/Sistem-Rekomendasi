name: myenv
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10  
  - pip
  - scikit-learn
  - scipy
  - pandas
  - numpy
  - nltk
  - streamlit
  - pip:
      - joblib
      - contractions
      - scikit-surprise
