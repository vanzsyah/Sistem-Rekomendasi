name: myenv
channels:
  - conda-forge
dependencies:
  - pip
  - scikit-surprise
  - pip:
      - cython
      - numpy
      - pandas
      - scipy
      - contractions
      - scikit-learn
      - nltk
      - joblib
      
